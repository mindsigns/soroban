<h2>Edit job</h2>

<%= render "form.html", changeset: @changeset,
                        services: @services,
                        action: job_path(@conn, :update, @job) %>

<%= link "Back", to: job_path(@conn, :index) %>
